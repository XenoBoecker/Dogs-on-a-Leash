using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DogInfo : MonoBehaviour
{
    Dictionary<int, string> dogSkins = new Dictionary<int, string>();

    // Update is called once per frame
    void Update()
    {
        
    }
}
